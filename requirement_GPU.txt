# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
opencv-python>=4.8.0
pillow>=10.0.0
websockets>=11.0.3
asyncio>=3.4.3

# YOLOv8 for person detection
ultralytics>=8.0.0
supervision>=0.18.0

# Face recognition
facenet-pytorch>=2.5.3
mtcnn>=0.1.0  # For MTCNN face detection (PyPI version if using standalone)

# TensorFlow (add version based on your setup)
tensorflow>=2.11.0  # Adjust version to match your CUDA/cuDNN setup if needed

# Logging and utilities
python-dotenv>=1.0.0

# Optional - CUDA support (uncomment if needed)
# torch-cuda  # specify version matching your CUDA installation
